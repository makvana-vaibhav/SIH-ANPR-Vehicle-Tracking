[pytest]
# The end-to-end suite talks to a running stack over HTTP. `auto` mode means
# async tests and fixtures work without decorating every one of them, matching
# how the unit suites are configured.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
