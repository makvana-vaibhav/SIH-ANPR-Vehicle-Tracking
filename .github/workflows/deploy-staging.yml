name: Deploy Staging

on:
  push:
    branches:
      - build/nagarnetra
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: []

    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.STAGING_EC2_HOST }}
          username: ${{ secrets.STAGING_EC2_USER }}
          key: ${{ secrets.STAGING_EC2_SSH_KEY }}
          script_stop: true
          script: |
            set -euo pipefail
            export GITHUB_REPOSITORY='${{ github.repository }}'
            export REPO_URL='https://github.com/${{ github.repository }}.git'
            if [ ! -d /opt/nagarnetra/staging/.git ]; then
              sudo mkdir -p /opt/nagarnetra
              sudo chown -R "$USER":"$USER" /opt/nagarnetra
              git clone --branch build/nagarnetra "$REPO_URL" /opt/nagarnetra/staging
            fi
            bash /opt/nagarnetra/staging/deploy/scripts/update_and_deploy.sh staging build/nagarnetra
